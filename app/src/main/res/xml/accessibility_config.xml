<?xml version="1.0" encoding="utf-8"?>
<!--
    AccessibilityService config for TrustShield.

    Why event-driven:
    - TYPE_WINDOW_STATE_CHANGED fires when a new browser tab/page becomes active.
    - TYPE_WINDOW_CONTENT_CHANGED fires when the address bar or page content updates.
    Together they cover every URL navigation without polling, keeping battery impact minimal.

    Package filters restrict event delivery to known browser processes only,
    so TrustShield never receives events from unrelated apps.
-->
<accessibility-service xmlns:android="http://schemas.android.com/apk/res/android"
    android:accessibilityEventTypes="typeWindowStateChanged|typeWindowContentChanged"
    android:accessibilityFlags="flagReportViewIds"
    android:canRetrieveWindowContent="true"
    android:packageNames="com.android.chrome,org.mozilla.firefox,com.sec.android.app.sbrowser"
    android:description="@string/accessibility_service_description" />
